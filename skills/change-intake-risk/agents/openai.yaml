interface:
  display_name: "Change Intake Risk"
  short_description: "Validate the contract and classify risk deterministically"
  default_prompt: "Use $change-intake-risk for this repository task and obey its authority and containment boundaries."
policy:
  allow_implicit_invocation: false
