# ZTAD Mesh 4.2.0 publication recovery

This temporary commit contains one verified materialization workflow. It reconstructs the validated source only from owner-authored, per-part checksummed GitHub issue comments, verifies the complete archive and final Git tree, then replaces itself with the permanent source and CI workflow.
