# ZTAD Mesh 4.2.0 publication recovery

This temporary commit contains one verified materialization workflow. It reconstructs the validated source from owner-authored GitHub issue comments, verifies every segment and part, validates the complete archive and final Git tree, and replaces itself with the permanent source and CI workflow.
